shallow_clone: true

os:
  - Visual Studio 2017

environment:
  matrix:
  - TOOLSET: vs2017

configuration:
  - debug-static
  - release-static

install:
  - cmd: git submodule update deps\poco
  - cmd: build\deploy_poco.cmd
  - cmd: build\genproj.cmd

build:
  project: trader.sln
