shallow_clone: true

os:
  - Visual Studio 2017

environment:
  matrix:
  - TOOLSET: vs2017

configuration:
  - debug-static
  - release-static

install:
  git submodule update deps\poco
  build\deploy_poco.cmd
  build\genproj.cmd

build:
  project: trader.sln
